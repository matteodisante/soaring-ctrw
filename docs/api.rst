API reference
=============

The whole public API of the :mod:`soaring_ctrw` package is documented below,
generated directly from the docstrings. The tree is **recursive**: every
submodule, class and function is discovered from the live package, so adding
or renaming any of them updates this reference automatically — there is no
hand-maintained list to keep in sync.

.. autosummary::
   :toctree: _generated/api
   :recursive:

   soaring_ctrw
